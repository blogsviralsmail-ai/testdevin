import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kkhsmedia.ludoliveking',
  appName: 'Ludo Live King',
  webDir: 'dist'
};

export default config;
